# Decisions

## 2026-07 — Jacob’s 5 templates only

Only these customer emails send: order+appointment confirmation (post-book), appointment reminder, results available (portal link), cancellation, password OTP. Payment-time confirmation and incomplete-test reminders retired.

## 2026-08 — Panel rollout order

Prove 5-panel with THC fully (LabCorp → MRO → DFC) before adding/configuring other panels.

## 2026-08-24 — AI hub location

Living memory for DFC lives at `~/mac/ai/vibe-coding/projects/slark/dfc/` (not inside the three app clones). Code stays at `~/mac/slark/dfc/*`.
