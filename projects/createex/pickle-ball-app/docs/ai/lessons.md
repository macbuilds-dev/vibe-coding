# Lessons

## Do not repeat
- Do not call `Get.snackbar` when Overlay may be missing — use `showCustomSnackbar`.
- Do not rely on a single Georgia default instructor for $12.50 CTA.
- AGP 9 + Gradle 9 upgrade breaks this project today (plugin compileSdk + Stripe tapandpay) — bump Kotlin only until plugins catch up.
